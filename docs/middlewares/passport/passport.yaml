info:
  title: "Middleware de Passport"
  description: "Middleware para autenticación y autorización utilizando Passport"

paths:
  /middlewares/passport:
    post:
      summary: Middleware de Passport - Autenticación Local
      description: Middleware para autenticar a un usuario utilizando estrategia de autenticación local
      tags:
        - Middleware
      responses:
        200:
          description: Usuario autenticado exitosamente
        401:
          description: Credenciales incorrectas o no autorizado
        500:
          description: Error interno del servidor

     
